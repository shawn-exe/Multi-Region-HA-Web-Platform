--- This is the old file for single region deployement ---
provider "aws" {
  region = "us-east-1"
}

module "networking" {
  source = "../../modules/networking"

  environment = "prod"

  vpc_cidr = var.vpc_cidr
  azs      = var.azs
}

